# DeskRelay Agent Instructions

- 이 저장소에서는 `rg` / `ripgrep`를 사용하지 않는다. 파일 검색은 PowerShell `Get-ChildItem`, `Select-String`, 또는 필요한 전용 도구로 처리한다.
- product 저장소와 self 저장소를 섞지 않는다. 이 저장소는 self-host DeskRelay만 다룬다.
