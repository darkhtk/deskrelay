{
  "name": "@deskrelay/site-backend",
  "version": "0.0.0",
  "private": true,
  "description": "Self-host Hono/Bun backend that proxies the browser to user-registered connector daemon URLs.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./app": "./src/app.ts",
    "./device-registry": "./src/device-registry.ts"
  },
  "bin": {
    "cr-site": "./src/bin.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "start": "bun run src/bin.ts"
  },
  "dependencies": {
    "@deskrelay/shared": "workspace:*",
    "hono": "^4.6.13"
  },
  "license": "Apache-2.0"
}
