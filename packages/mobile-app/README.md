# DeskRelay Mobile Shell

This package is a local wrapper around the built web UI. It is optional for
self-host users; the normal path is to open the DeskRelay web UI from a browser
and point it at your own site backend.

The wrapper does not provide a hosted account, managed distribution, commercial
flow, or managed relay service.
